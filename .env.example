# Optional overrides (copy to `.env.local`). If you omit it:
# - `npm run dev` uses ws://localhost:3334 (see `.env.development`)
# - `npm run build` uses wss://moba-rooms.fly.dev (see `.env.production`)
#
# VITE_ROOM_WS_URL=ws://localhost:3334
# VITE_ROOM_WS_URL=wss://your-custom-host.example
